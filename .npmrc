# Next.js canary builds use prerelease versions (e.g. 16.x-canary.x) which
# npm strictly excludes from semver ranges like ">=13" declared as peer
# dependency by @vercel/analytics, @vercel/speed-insights, etc.
# Falling back to the npm 6 resolution algorithm avoids the false ERESOLVE.
legacy-peer-deps=true
